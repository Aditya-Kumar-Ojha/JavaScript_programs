// if we declare a const varibale it must assigne to value , 
//once const varibale assigne it can't be change , 
//it is a block level varibale.
const x=12;
console.log(x);
//prpperty of the const object can be change 
const obj={
    y:1
};

obj.y++;

console.log(obj);

